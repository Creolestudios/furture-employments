import { styled } from 'styled-components';

const StyledApplicationDetails = styled.div`
  .card-container {
    margin-bottom: 20px;
  }

  p {
    line-height: 1.5;
    margin-bottom: 10px;
  }
`;

export default StyledApplicationDetails;
